const db = require("./databaseConn");
const { signUp, login } = require("./verify");
const validate = require("express-validator");
const bcrypt = require("bcryptjs");

async function signUp(req, req, next) {}

const signUp = async;
